import React from 'react';

import {Text, View} from 'react-native';

import styles from './style';

const Header = () => {
  return (
    <View>
      <Text style={styles.txt}>SL Corona Tracker</Text>
    </View>
  );
};

export default Header;
